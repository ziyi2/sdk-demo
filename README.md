# ziyi-sdk-demo

安装

``` bash
npm i ziyi-sdk-demo --save
```

使用

``` javascript
// ES Module 引入
import { add } from 'ziyi-sdk-demo';
add(1, 2);

// CommonJS 引入
const  { add } = require('ziyi-sdk-demo');
add(1, 2);
```